server:
  port: 8083

spring:
  datasource:
    username: sa
    url: jdbc:h2:mem:testdb

keycloak:
  server:
    contextPath: /auth
    adminUser:
      username: deduper-admin
      password: pass
    realmImportFile: deduper_answer-realm.json
